#include <iostream>
#include <fstream>
#include <map>
#include <vector>
#include <string>
#include <iomanip>
#include <sstream>

using std::cout;
using std::cin;
using std::string;
using std::map;
using std::ofstream;
using std::ifstream;
using std::getline;
using std::vector;
using std::pair;
using std::ios;

std::fstream logOfAccounts;
std::stringstream stringStream;